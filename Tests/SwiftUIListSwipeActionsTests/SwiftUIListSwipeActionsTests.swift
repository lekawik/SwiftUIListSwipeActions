import XCTest
@testable import SwiftUIListSwipeActions

final class SwiftUIListSwipeActionsTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftUIListSwipeActions().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
